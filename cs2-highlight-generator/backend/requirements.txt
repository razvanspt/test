# Python dependencies for CS2 Highlight Generator Backend
# Install with: pip install -r requirements.txt

# Web framework (similar to Spring Boot in Java)
fastapi==0.104.1
uvicorn[standard]==0.24.0  # ASGI server (like Tomcat for Java)

# CS2 Demo parsing library (no Java equivalent exists)
awpy>=2.0.2  # Latest version with improved parsing
demoparser2>=0.40.2  # Direct access to parser for FaceIt demos

# File handling and utilities
python-multipart==0.0.6  # For file uploads
aiofiles==23.2.1  # Async file operations

# Data validation (similar to Java Bean Validation)
pydantic==2.5.0

# JSON handling
orjson==3.9.10  # Faster JSON parsing

# CORS middleware (for frontend communication)
# Note: CORS is built into FastAPI, no additional package needed

# Type checking (optional but recommended)
mypy==1.7.1
